NODE_ENV=development
PORT=5000

# PostgreSQL connection string (Prisma)
DATABASE_URL="postgresql://neondb_owner:npg_zd6KhjXHqc5s@ep-flat-mouse-az3y6u9s-pooler.c-3.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# JWT secrets - generate strong random values for production
JWT_ACCESS_SECRET="replace_with_long_random_string"
JWT_REFRESH_SECRET="replace_with_another_long_random_string"
JWT_ACCESS_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="30d"

# Google OAuth (GCP) - client ID from Google Cloud Console for social login
GOOGLE_CLIENT_ID=""

# Stripe (test mode keys from https://dashboard.stripe.com/test/apikeys)
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""

# Redis (optional - falls back to in-memory cache if unset)
REDIS_URL=""
 # Get a free instance at https://console.upstash.com (Redis protocol URL, not REST API URL)

CORS_ORIGIN="*"

# Demo admin credentials created by the seed script
ADMIN_EMAIL="admin@courierhub.com"
ADMIN_PASSWORD="Admin@12345"
